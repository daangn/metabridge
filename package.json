{
  "name": "@metabridge/monorepo",
  "version": "0.0.0",
  "workspaces": [
    "cli",
    "plugins/*",
    "schema"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -t run build",
    "test": "yarn workspaces foreach -t run test",
    "version": "yarn build && lerna version --no-git-tag-version"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.5.1",
    "typescript": "^4.6.2"
  },
  "packageManager": "yarn@3.2.0"
}
